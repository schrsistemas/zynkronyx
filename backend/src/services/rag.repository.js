const db=require('./db.service');
async function nextId(tx,g){const sequenceMap={GEN_AI_DOCUMENT_ID:'AI_DOCUMENT',GEN_AI_INGESTION_JOB_ID:'AI_INGESTION_JOB',GEN_AI_DOCUMENT_CHUNK_ID:'AI_DOCUMENT_CHUNK'};const table=sequenceMap[g];if(!table){const e=new Error('UNKNOWN_ID_SEQUENCE');e.code='UNKNOWN_ID_SEQUENCE';throw e;}const rows=await tx.query('SELECT MAX(ID) AS ID FROM '+table);return Number(rows[0].ID||0)+1;}
async function getDocumentByKey(tx,t,k){const r=await tx.query('SELECT ID,TENANT_ID,EXTERNAL_KEY,VERSION_NO,CHECKSUM_SHA256,STATUS FROM AI_DOCUMENT WHERE TENANT_ID=? AND EXTERNAL_KEY=?',[t,k]);return r[0]||null;}
async function getJobByIdempotency(tx,t,k){const r=await tx.query('SELECT ID,TENANT_ID,DOCUMENT_ID,IDEMPOTENCY_KEY,STATUS,STAGE,ERROR_CODE,ERROR_MESSAGE,CORRELATION_ID,CREATED_AT,STARTED_AT,FINISHED_AT FROM AI_INGESTION_JOB WHERE TENANT_ID=? AND IDEMPOTENCY_KEY=?',[t,k]);return r[0]||null;}
async function claimNextJob(){return db.withTransaction(async tx=>{const r=await tx.query("SELECT FIRST 1 ID,TENANT_ID,DOCUMENT_ID,IDEMPOTENCY_KEY,STATUS,STAGE,CORRELATION_ID FROM AI_INGESTION_JOB WHERE STATUS='QUEUED' ORDER BY ID WITH LOCK");const j=r[0];if(!j)return null;await tx.execute("UPDATE AI_INGESTION_JOB SET STATUS='RUNNING',STAGE='PROCESSING',STARTED_AT=CURRENT_TIMESTAMP,ERROR_CODE=NULL,ERROR_MESSAGE=NULL WHERE ID=? AND STATUS='QUEUED'",[j.ID]);return {...j,STATUS:'RUNNING',STAGE:'PROCESSING'};});}
async function getJobChunks(job){return db.query('SELECT ID,TENANT_ID,DOCUMENT_ID,CHUNK_INDEX,CONTENT,CONTENT_HASH,TOKEN_COUNT,VECTOR_KEY,EMBEDDING_MODEL,STATUS FROM AI_DOCUMENT_CHUNK WHERE TENANT_ID=? AND DOCUMENT_ID=? ORDER BY CHUNK_INDEX',[job.TENANT_ID,job.DOCUMENT_ID]);}
async function markChunkEmbedding(id,embeddingModel,vectorKey){return db.execute("UPDATE AI_DOCUMENT_CHUNK SET EMBEDDING_MODEL=?,VECTOR_KEY=?,STATUS='EMBEDDED' WHERE ID=?",[embeddingModel||null,vectorKey||null,id]);}
async function markChunkIndexed(id,vectorKey){return db.execute("UPDATE AI_DOCUMENT_CHUNK SET VECTOR_KEY=?,STATUS='INDEXED' WHERE ID=?",[vectorKey||null,id]);}
async function lexicalRetrieve(tenantId,query,topK){const terms=[...new Set(String(query).toUpperCase().split(/[^A-Z0-9À-ÿ]+/).filter(t=>t.length>=3))].slice(0,8);if(!terms.length)return[];const clauses=terms.map(()=>'(UPPER(C.CONTENT) CONTAINING ?)').join(' OR ');const rows=await db.query('SELECT FIRST 50 C.ID,C.TENANT_ID,C.DOCUMENT_ID,C.CHUNK_INDEX,C.CONTENT,C.CONTENT_HASH,C.TOKEN_COUNT,D.TITLE,D.EXTERNAL_KEY,D.ACL_JSON FROM AI_DOCUMENT_CHUNK C JOIN AI_DOCUMENT D ON D.ID=C.DOCUMENT_ID AND D.TENANT_ID=C.TENANT_ID WHERE C.TENANT_ID=? AND D.STATUS=\'ACTIVE\' AND ('+clauses+') ORDER BY C.DOCUMENT_ID,C.CHUNK_INDEX',[tenantId,...terms]);return rows.slice(0,topK).map(r=>({chunk_id:Number(r.ID),document_id:Number(r.DOCUMENT_ID),chunk_index:Number(r.CHUNK_INDEX),content:r.CONTENT,title:r.TITLE,external_key:r.EXTERNAL_KEY,acl_json:r.ACL_JSON,token_count:Number(r.TOKEN_COUNT||0),score:terms.reduce((n,t)=>n+(String(r.CONTENT).toUpperCase().includes(t)?1:0),0),source:'lexical'})).sort((a,b)=>b.score-a.score).slice(0,topK);}
async function finishJob(id,status,stage,code=null,message=null){return db.execute('UPDATE AI_INGESTION_JOB SET STATUS=?,STAGE=?,ERROR_CODE=?,ERROR_MESSAGE=?,FINISHED_AT=CURRENT_TIMESTAMP WHERE ID=?',[status,stage,code,message,id]);}
async function createIngestion(input){const {tenantId,sourceType,externalKey,title,checksum,classification,aclJson,metadataJson,correlationId,idempotencyKey,chunks}=input;return db.withTransaction(async tx=>{const oldJob=await getJobByIdempotency(tx,tenantId,idempotencyKey);if(oldJob)return{created:false,idempotent:true,documentId:oldJob.DOCUMENT_ID,job:oldJob};const old=await getDocumentByKey(tx,tenantId,externalKey);let documentId,versionNo=1;if(old&&old.CHECKSUM_SHA256===checksum&&old.STATUS==='ACTIVE'){documentId=Number(old.ID);versionNo=Number(old.VERSION_NO);}else{documentId=old?Number(old.ID):await nextId(tx,'GEN_AI_DOCUMENT_ID');versionNo=old?Number(old.VERSION_NO)+1:1;if(old){await tx.execute('UPDATE AI_DOCUMENT SET VERSION_NO=?,CHECKSUM_SHA256=?,TITLE=?,CLASSIFICATION=?,ACL_JSON=?,METADATA_JSON=?,STATUS=?,UPDATED_AT=CURRENT_TIMESTAMP,DELETED_AT=NULL WHERE ID=? AND TENANT_ID=?',[versionNo,checksum,title||null,classification,aclJson,metadataJson,'ACTIVE',documentId,tenantId]);await tx.execute('DELETE FROM AI_DOCUMENT_CHUNK WHERE DOCUMENT_ID=? AND TENANT_ID=?',[documentId,tenantId]);}else await tx.execute('INSERT INTO AI_DOCUMENT (ID,TENANT_ID,SOURCE_TYPE,EXTERNAL_KEY,TITLE,VERSION_NO,CHECKSUM_SHA256,CLASSIFICATION,ACL_JSON,STATUS,METADATA_JSON) VALUES (?,?,?,?,?,?,?,?,?,?,?)',[documentId,tenantId,sourceType,externalKey,title||null,versionNo,checksum,classification,aclJson,'ACTIVE',metadataJson]);}const jobId=await nextId(tx,'GEN_AI_INGESTION_JOB_ID');for(const c of chunks){const id=await nextId(tx,'GEN_AI_DOCUMENT_CHUNK_ID');await tx.execute('INSERT INTO AI_DOCUMENT_CHUNK (ID,TENANT_ID,DOCUMENT_ID,CHUNK_INDEX,CONTENT,CONTENT_HASH,TOKEN_COUNT,VECTOR_KEY,EMBEDDING_MODEL,STATUS) VALUES (?,?,?,?,?,?,?,?,?,?)',[id,tenantId,documentId,c.index,c.content,c.hash,c.tokenCount,null,null,'PENDING']);}await tx.execute('INSERT INTO AI_INGESTION_JOB (ID,TENANT_ID,DOCUMENT_ID,IDEMPOTENCY_KEY,STATUS,STAGE,CORRELATION_ID) VALUES (?,?,?,?,?,?,?)',[jobId,tenantId,documentId,idempotencyKey,'QUEUED','CHUNKED',correlationId||null]);return{created:true,idempotent:false,documentId,versionNo,chunkCount:chunks.length,jobId,status:'QUEUED',stage:'CHUNKED'};});}
module.exports={createIngestion,getDocumentByKey,getJobByIdempotency,claimNextJob,getJobChunks,markChunkEmbedding,markChunkIndexed,lexicalRetrieve,finishJob};